---
title: Password
description: Sign users in with a username and a password.
---

import { Step, Steps } from "fumadocs-ui/components/steps";

The password component lets you implement password-based login. It automatically handles subtle security details of password handling, including hashing (with Argon2id), rate limiting, and validation.

{/* TODO(nicolas) Include doc about password validation rules */}

## Setting up username + password login in your app

<Steps>

<Step>

### Follow the “Getting Started” guide

First, follow the [Getting Started](/getting-started) guide:

<Cards>
  <Card
    href="/getting-started"
    title="Getting Started"
    description="Installing dependencies + initial set up."
  />
</Cards>

</Step>

<Step>

### Install the components

Register the password component and the username component alongside the core
in `convex/convex.config.ts`. The username component stores which user each
username identifies:

<include
  lang="ts"
  meta='title="convex/convex.config.ts" highlight="import passwordProvider from" highlight="import username from" highlight="app.use(passwordProvider)" highlight="app.use(username)"'
>
  ../../../../../examples/react-password/convex/convex.config.ts
</include>

</Step>

<Step>

### Add a `users` table to the schema

Add a `users` table to your schema in `convex/schema.ts` with a `username` string field.

Convex Auth will create a row for each account. You can also add application-specific fields to this table if necessary.

The table does not have to be called `users` — if yours has another name, pass
it to `setupCore`, and every provider's `attachUserCallbacks` will then require
callbacks typed with that table's ids:

```ts title="convex/auth.ts"
const core = setupCore({ component: components.auth, usersTable: "members" });
```

<include
  lang="ts"
  meta='title="convex/schema.ts" highlight="users: defineTable({...}),"'
>
  ../../../../../examples/react-password/convex/schema.ts
</include>

</Step>

<Step>

### Set up user creation

Write a `createUser` mutation that creates a row in your `users` table and
returns its id. The provider calls it the first time it sees an account.

You can also attach an optional `onSignIn` mutation, called with the resolved
`userId` on every sign-in, the first one included. Anything you want to happen
each time someone signs in belongs there, so `createUser` only has to handle
what is true at creation time. Leave it out if there is nothing to do.

<include lang="ts" meta='title="convex/users.ts"'>
  ../../../../../examples/react-password/convex/users.ts
</include>

</Step>

<Step>

### Set up the public backend functions

Set up the password provider with `setupUsernamePassword` and export the
sign-up and sign-in mutations it hands back once you attach the callback that
creates your user rows, in `convex/auth.ts`:

<include
  lang="ts"
  meta='title="convex/auth.ts" highlight="import { setupUsernamePassword }" highlight="export const { signUpWithPassword, signInWithPassword } = setupUsernamePassword(...).attachUserCallbacks({ createUser: internal.users.createUser });"'
>
  ../../../../../examples/react-password/convex/auth.ts
</include>

</Step>

<Step>

### Set up the provider on the client

Wrap your app in `ConvexAuthProvider`, passing it the `refreshSession` and
`signOut` mutations in `src/main.tsx`:

<include
  lang="tsx"
  meta='title="src/main.tsx" highlight="import { ConvexAuthProvider }" highlight="<ConvexAuthProvider...>" highlight="</ConvexAuthProvider>"'
>
  ../../../../../examples/react-password/src/main.tsx
</include>

</Step>

<Step>

### Set up the authentication UI

In your front-end, set up routes for logging in and creating an account.

Copy the following code blocks inside of your app:

#### Sign up

<include lang="tsx" meta='title="src/routes/signUp.tsx"'>
  ../../../../../examples/react-password/src/routes/signUp.tsx
</include>

#### Log in

<include lang="tsx" meta='title="src/routes/logIn.tsx"'>
  ../../../../../examples/react-password/src/routes/logIn.tsx
</include>

</Step>

</Steps>
