---
title: Passkey
description: Sign users in with a username and a passkey.
---

import { Step, Steps } from "fumadocs-ui/components/steps";

The passkey component lets you implement passkey-based login.

A passkey replaces the password with a key pair that stays on the user's device:
users can store the passkey on their device (protected by a fingerprint,
a face scan, or PIN), or use an external hardware security key.
Your app never handles a secret that can be phished or reused.

The `setupUsernamePasskey` provider implements an _identifier-first_ flow:
your login page has a single username field. A free username creates a new
account with a new passkey; an existing username asks for a passkey of that
account.

## Setting up username + passkey login in your app

<Steps>

<Step>

### Follow the “Getting Started” guide

First, follow the [Getting Started](/getting-started) guide:

<Cards>
  <Card
    href="/getting-started"
    title="Getting Started"
    description="Installing dependencies + initial set up."
  />
</Cards>

</Step>

<Step>

### Install the components

Register the passkey component and the username component alongside the core
in `convex/convex.config.ts`. The username component stores which user each
username identifies:

<include
  lang="ts"
  meta='title="convex/convex.config.ts" highlight="import passkey from" highlight="import username from" highlight="app.use(passkey)" highlight="app.use(username)"'
>
  ../../../../../examples/react-passkey-basic/convex/convex.config.ts
</include>

</Step>

<Step>

### Add a `users` table to the schema

Add an empty `users` table to your schema in `convex/schema.ts`. The username
component keeps the username, so the table does not need a field for it.

Convex Auth will create a row for each account. You can also add application-specific fields to this table if necessary.

<include
  lang="ts"
  meta='title="convex/schema.ts" highlight="users: defineTable({}),"'
>
  ../../../../../examples/react-passkey-basic/convex/schema.ts
</include>

</Step>

<Step>

### Set up user creation

Write a `createUser` mutation that creates a row in your `users` table and
returns its id. The provider calls it the first time it sees an account.

You can also attach an optional `onSignIn` mutation, called with the resolved
`userId` on every sign-in, the first one included. Anything you want to happen
each time someone signs in belongs there, so `createUser` only has to handle
what is true at creation time. Leave it out if there is nothing to do.

<include lang="ts" meta='title="convex/users.ts"'>
  ../../../../../examples/react-passkey-basic/convex/users.ts
</include>

</Step>

<Step>

### Set up the public backend functions

Set up the passkey provider with `setupUsernamePasskey` and export the four
mutations it hands back once you attach the callback that creates your user
rows, in `convex/auth.ts`:

<include
  lang="ts"
  meta='title="convex/auth.ts" highlight="import { setupUsernamePasskey }" highlight="export const { startSignIn, startAutofillSignIn, finishSignUp, finishSignIn } =...}).attachUserCallbacks({ createUser: internal.users.createUser });"'
>
  ../../../../../examples/react-passkey-basic/convex/auth.ts
</include>

`rpId` and `origin` bind the passkeys to your app:

- `rpId` is the _relying party ID_: the domain the passkeys belong to, without
  a scheme or a port (`localhost` in development, `example.com` in
  production). Browsers only offer a passkey to a page whose domain is the
  `rpId` or a subdomain of it.
- `origin` is the full origin the login page is served from, including the
  scheme and the port (`http://localhost:5173` for the Vite dev server). The
  component rejects a passkey sign-in that comes from any other origin.

<Callout type="warn">
  Passkeys are permanently bound to their `rpId`: a passkey created with `rpId:
  "localhost"` can never be used on your production domain. Set both values to
  the production domain before you deploy.
</Callout>

</Step>

<Step>

### Set up the provider on the client

Wrap your app in `ConvexAuthProvider`, passing it the `refreshSession` and
`signOut` mutations in `src/main.tsx`:

<include
  lang="tsx"
  meta='title="src/main.tsx" highlight="import { ConvexAuthProvider }" highlight="<ConvexAuthProvider...>" highlight="</ConvexAuthProvider>"'
>
  ../../../../../examples/react-passkey-basic/src/main.tsx
</include>

</Step>

<Step>

### Set up the authentication UI

In your front-end, set up a route for logging in. Because the flow is
identifier-first, the same page and the same field serve account creation and
login.

While the hook is mounted, it also lets the browser offer the passkeys it has
stored in the autocompletion list of the username field. The field must carry
`autoComplete="username webauthn"` for the browser to do that.

<Callout type="warn">
  The browser allows only one of these autofill requests at a time, so you
  should not mount the `usePasskey` hook multiple times simultaneously.
</Callout>

Copy the following code block inside of your app:

<include lang="tsx" meta='title="src/routes/logIn.tsx"'>
  ../../../../../examples/react-passkey-basic/src/routes/logIn.tsx
</include>

</Step>

</Steps>
