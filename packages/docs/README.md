# Convex Auth v2 — Docs

Run the docs with:

```bash
pnpm run --filter docs dev
```

TODO: We will probably include these docs as part of https://docs.convex.dev instead of having a standalone Fumadocs project when Convex Auth v2 is released.
