/**
 * The anonymous provider's conventional route subpath, shared by its
 * `anonymousRoutes` server helper and its SSR client hook.
 *
 * @module
 */

/** Subpath of the anonymous sign-in route under the auth base path. */
export const ANONYMOUS_SIGN_IN_PATH = "signin/anonymous";
