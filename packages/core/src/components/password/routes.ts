/**
 * The password provider's conventional route subpaths, shared by its
 * `passwordRoutes` server helper and its SSR client hooks.
 *
 * @module
 */

/** Subpath of the password sign-in route under the auth base path. */
export const PASSWORD_SIGN_IN_PATH = "signin/password";

/** Subpath of the password sign-up route under the auth base path. */
export const PASSWORD_SIGN_UP_PATH = "signup/password";
