[package]
name = "argon2-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "=0.2.100"   # exact pin so wasm-pack fetches a matching CLI
argon2 = { version = "0.5", features = ["password-hash"] }
